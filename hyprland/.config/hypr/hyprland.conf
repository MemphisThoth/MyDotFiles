monitor=,preferred,auto,1

$terminal = kitty
$menu = fuzzel

exec-once = waybar
exec-once = mako
exec-once = awww-daemon
exec-once = sleep 1 && awww img ~/Pictures/Wallpaper/wp14806918.webp

bind = SUPER, Return, exec, $terminal
bind = SUPER, D, exec, $menu
bind = SUPER, Q, killactive
bind = SUPER, M, exit
bind = SUPER_SHIFT, L, exec, hyprlock
bind = SUPER, E, exec, thunar

# Switch workspaces
bind = SUPER, 1, workspace, 1
bind = SUPER, 2, workspace, 2
bind = SUPER, 3, workspace, 3
bind = SUPER, 4, workspace, 4
bind = SUPER, 5, workspace, 5
bind = SUPER, 6, workspace, 6
bind = SUPER, 7, workspace, 7
bind = SUPER, 8, workspace, 8
bind = SUPER, 9, workspace, 9

# Move window to workspace
bind = SUPER SHIFT, 1, movetoworkspace, 1
bind = SUPER SHIFT, 2, movetoworkspace, 2
bind = SUPER SHIFT, 3, movetoworkspace, 3
bind = SUPER SHIFT, 4, movetoworkspace, 4
bind = SUPER SHIFT, 5, movetoworkspace, 5
bind = SUPER SHIFT, 6, movetoworkspace, 6
bind = SUPER SHIFT, 7, movetoworkspace, 7
bind = SUPER SHIFT, 8, movetoworkspace, 8
bind = SUPER SHIFT, 9, movetoworkspace, 9

# Change focus
bind = SUPER, J, movefocus, l
bind = SUPER, K, movefocus, u
bind = SUPER, H, movefocus, r
bind = SUPER, L, movefocus, d


#layerrulev2 = blur, class:^(waybar)$
#layerrulev2 = ignorezero, class:^(waybar)$

input {
    kb_layout = us
}

general {
    gaps_in = 5
    gaps_out = 10
    border_size = 2
    col.active_border = rgb(2b7fff)
    col.inactive_border = rgb(223344)
}

decoration {
    rounding = 10

    blur {
        enabled = true
        size = 8
        passes = 3
        vibrancy = 0.2
    }
}

animations {
    enabled = yes
}
